Elements and Tags

1. What is an HTML tag?
- HTML tag is a container for an element in the web page

2. What are the three parts of an HTML element?
- Opening tag, content, closing tag

HTML Boilerplate

1. What is the purpose of the doctype declaration?
- To tell the browser what version of HTML it should use to render the document

2. What is the HTML element?
- The root element of the web page.

3. What is the purpose of the head element?
- Contains all the meta information, title tag, stylesheets, and scripts.

4. WHat is the purpose of the body element?
- Contains all the information that will be displayed to the web page

Working with Text
    How do you create a paragraph in HTML?
    - with the p tag
    How do you create a heading in HTML?
    - with h1 to h6 tag
    How many different levels of headings are there and what is the difference between them?
    - six, difference in emphasis and size
    What element should you use to make text bold and important?
    - strong tag
    What element should you use to make text italicized to add emphasis to it?
    - em tag
    What relationship does an element have with any nested elements within it?
    - an element becomes a parent when it contains a child element inside it
    What relationship do two elements have if they are at the same level of nesting?
    - they are sibling elements
    How do you create HTML comments?
    - with <!-- -->

Lists

    What HTML tag is used to create an unordered list?
    - ul + li tag
    What HTML tag is used to create an ordered list?
    - ol + li tag
    What HTML tag is used to create list items within both unordered and ordered lists?
    - li tag

links and images

    What element is used to create a link?
    - a element
    What is an attribute?
    - an attribute is something that gives an element extra definitions
    What attribute tells links where to go to?
    - href
    What is the difference between an absolute and relative link?
    - absolute link goes to pages of a different website, relative goes to pages of your own website
    Which element is used to display an image?
    - img
    What two attributes do images always need to have?
    - src and alt
    How do you access a parent directory in a filepath?
    - ../
    What are the four main image formats that you can use for images on the web?
    - jpg=best for photos, gif=best for animations, png=best for icons and diagrams, svg=best for responsive design

css foundations


    What are the main differences between external, internal, and inline CSS?
    - external allows you to link a separate .css file in your html file through a single line, internal lets you add the style declarations inside the html file, and inline lets you style your elements inside of the elements themselves

    What is the syntax for class and ID selectors?
    - class="" ,, .class-name {} ,, id="" ,, #id-name {}

    How would you apply a single rule to two different selectors?
    - .class1, .class2 {}
    
    Given an element that has an id of title and a class of primary, how would you use both attributes for a single rule?
    - #title.primary

    What does the descendant combinator do?
    - lets you select a specific child based on ancestral relationships from the designated child up to the first ancestor

    Between a rule that uses one class selector and a rule that uses three type selectors, which rule has the higher specificity?
    - class selector

Inspecting HTML and CSS

    How do you select a specific element on your page with your browser’s developer tools?
    - ctrl+shift+c

    What does a strikethrough in a CSS element mean in your browser’s developer tools?
    - style that is overwritten
    
    How do you change CSS in real time on specific elements of a web page with your browser’s developer tools?
    - ctrl+shift+c > hover and click on the element > click on the inside of the element.style in styles pane > enter css property and value

The Box Model

    From inside to outside, what is the order of box-model properties?
    - content > padding > border > margin
    
    What does the box-sizing CSS property do?
    - changes the box model to the alternative one

    What is the difference between the standard and alternative box model?
    - standard model adds the padding and border sizes to the specified width and height, while alternative model restricts the size of the entire box to the specified width and height

    Would you use margin or padding to create more space between 2 elements?
    - margin

    Would you use margin or padding to create more space between the contents of an element and its border?
    - padding

    Would you use margin or padding if you wanted two elements to overlap each other?
    - negative margin